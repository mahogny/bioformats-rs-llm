[package]
name = "bioformats-ome"
version = "0.1.0"
edition = "2021"
description = "OME-XML format reader (inline Base64 pixel data)"

[dependencies]
bioformats-common = { path = "../bioformats-common" }
log = { workspace = true }
