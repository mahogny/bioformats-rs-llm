[package]
name = "bioformats-xrm"
version = "0.1.0"
edition = "2021"
description = "Zeiss XRM X-ray tomography format reader"

[dependencies]
bioformats-common = { path = "../bioformats-common" }
log = { workspace = true }
