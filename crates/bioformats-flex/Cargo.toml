[package]
name = "bioformats-flex"
version = "0.1.0"
edition = "2021"
description = "PerkinElmer FLEX HCS format reader (TIFF-based)"

[dependencies]
bioformats-common = { path = "../bioformats-common" }
bioformats-tiff   = { path = "../bioformats-tiff" }
log = { workspace = true }
